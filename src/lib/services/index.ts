/**
 * Services index
 *
 * This module provides organized access to all application services.
 * Services are categorized into client-side and server-side modules.
 */

// Client-side services (browser-only functionality)
export * from './client';

// Server-side services are kept in their existing location at src/lib/server/
// and should be imported directly from there when needed
