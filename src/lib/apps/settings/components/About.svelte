<script lang="ts">
	import Logo from '$lib/assets/logo.png?enhanced';
	import { Mail, Globe, Package, ChevronRight } from 'lucide-svelte';
	import ContentSection from '$lib/components/shared/ContentSection.svelte';

	interface Props {
		// Jövőbeli props-ok ide kerülnek
	}

	let {}: Props = $props();

	const version = '0.0.2';
	const contactEmail = 'info@elyos.hu';
	const website = 'https://elyos.hu';
	const year = new Date().getFullYear();

	const changelog = [
		{
			version: '0.0.2',
			date: '2025-01-25',
			title: 'About oldal fejlesztések'
		},
		{
			version: '0.0.1',
			date: '2025-01-25',
			title: 'Kezdeti verzió'
		}
	];
</script>

<div class="flex h-full flex-col justify-between">
	<div class="flex flex-1 flex-col items-center justify-center space-y-8 text-center">
		<!-- Logo -->
		<div class="flex justify-center">
			<enhanced:img src={Logo} alt="ElyOS Logo" width="220" />
		</div>

		<!-- Verzió -->
		<div class="flex items-center justify-center gap-2 text-muted-foreground">
			<Package class="h-4 w-4" />
			<span class="text-sm">Verzió {version}</span>
		</div>

		<!-- Leírás -->
		<p class="mx-auto max-w-md text-muted-foreground">
			Modern webes asztali környezet a hatékony munkavégzéshez.
		</p>

		<!-- Kapcsolat és linkek -->
		<div class="flex flex-wrap items-center justify-center gap-4">
			<!-- Email -->
			<a
				href="mailto:{contactEmail}"
				class="flex items-center gap-2 rounded-lg bg-black/5 px-4 py-2 text-sm transition-colors hover:bg-black/10 dark:bg-white/5 dark:hover:bg-white/10"
			>
				<Mail class="h-4 w-4" />
				<span>{contactEmail}</span>
			</a>

			<!-- Weboldal -->
			<a
				href={website}
				target="_blank"
				rel="noopener noreferrer"
				class="flex items-center gap-2 rounded-lg bg-black/5 px-4 py-2 text-sm transition-colors hover:bg-black/10 dark:bg-white/5 dark:hover:bg-white/10"
			>
				<Globe class="h-4 w-4" />
				<span>elyos.hu</span>
			</a>
		</div>

		<!-- Changelog Section -->
		<div class="mt-8 w-full max-w-md">
			<ContentSection title="" description="Verzió előzmények">
				{#snippet info()}
					<div class="space-y-2">
						{#each changelog as entry}
							<button
								class="flex w-full items-center justify-between rounded-lg bg-white/5 p-3 text-left transition-colors hover:bg-white/10"
							>
								<div class="flex items-center gap-3">
									<span class="text-sm font-medium">v{entry.version}</span>
									<span class="text-xs text-muted-foreground">{entry.title}</span>
								</div>
								<div class="flex items-center gap-2">
									<span class="text-xs text-muted-foreground">{entry.date}</span>
									<ChevronRight class="h-4 w-4 text-muted-foreground" />
								</div>
							</button>
						{/each}
					</div>
				{/snippet}
			</ContentSection>
		</div>
	</div>

	<!-- Footer -->
	<div class="my-4 text-center text-xs text-muted-foreground">
		<p>&copy; {year} ElyOS</p>
	</div>
</div>
